environments:
  - name: dev
    endpoints:
      production:
        url: http://localhost:9443/samplestore
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: http://dev.sandbox.wso2.com:9090/samplestore
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: http://localhost:9444/samplestore
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: http://prod.sandbox.wso2.com:9090/samplestore
        config:
          retryTimeOut: $RETRY      
